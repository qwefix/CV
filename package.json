{
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "devClient": "cd client && npm run start",
    "devServer": "npx nodemon ./server/src/index.ts ",
    "build": "tsc && cd client && npm install && npm run build",
    "start": "node ./server_dist/index.js"
  }
}
